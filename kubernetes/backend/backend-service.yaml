apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: muchtodo-ns
spec:
  type: NodePort # This satisfies requirement #5
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080 # This makes it accessible to your host
