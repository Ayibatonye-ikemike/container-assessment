apiVersion: v1
kind: Secret
metadata:
  name: backend-env-secret
  namespace: muchtodo-ns
type: Opaque
stringData:
  .env: |
    PORT=8080
    MONGO_URI=mongodb://admin:password@muchtodo-db:27017
    DB_NAME=muchtodo
    JWT_SECRET_KEY=StartupTech_2026_Secret
